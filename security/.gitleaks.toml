title = "Gitleaks config (course project)"

[extend]
useDefault = true

[allowlist]
description = "Allow dev-only placeholders and config defaults"
paths = [
  "app/config.py",
  "tests/",
]
regexes = [
  "dev-secret-key",
  "Password123",
]
