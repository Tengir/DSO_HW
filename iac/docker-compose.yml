version: "3.9"
services:
  app:
    build: ..
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=dev
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
